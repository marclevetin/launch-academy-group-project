import React from 'react';
import EggsContainer from '../containers/EggsContainer';


const Layout = (props) => {
  return(
    <div>
      
      { props.children }
    </div>
  )
}

export default Layout;
